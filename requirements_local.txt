# Requirements for main_pred.py (predictions only)
# Minimal dependencies - no PyTorch needed!
# 
# Based on Colab versions (Python 3.12+)
# Compatible with Windows/Linux
#
# Install: pip install -r requirements_local.txt

# Core libraries
pandas>=2.2.0
numpy>=2.0.0
lightgbm>=4.5.0
scikit-learn>=1.6.0
joblib>=1.5.0

# Optional (for progress bars in some versions)
tqdm>=4.65.0

